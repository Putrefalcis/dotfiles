https://wiki.gentoo.org/wiki/Kernel/Upgrade

# cd /usr/src/linux
# cp .config ~/kernel-config-`uname -r`

# eselect kernel list
# eselect kernel set 2
# genkernel all OR

# cp /usr/src/linux-X.X.OLD-gentoo/.config /usr/src/linux/
# ln -sf /usr/src/linux-X.X.NEW-gentoo /usr/src/linux
# ls -l /usr/src/linux
# make silentoldconfig
# make -j9 (cores+1)
# make modules_install
# make install

# mount /boot
# grub2-mkconfig -o /boot/grub/grub.cfg

(+ Remove old kernel/initramfs from boot if too many fallbacks)

# emerge @module-rebuild

(old # emerge -av nvidia-drivers)
